"""
Legacy modules kept around for reference while v2 is under construction.

Currently holds the v1 `organic_rankings.py` standalone Streamlit page.
Phase 3 will port the useful parts (weighted-rank trends, position
distribution, page scorecard, keyword detail) into
`app/tabs/organic_deep_dive.py`. Once that port lands these files can be
deleted.
"""
